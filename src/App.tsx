import { TextInput } from './components/TextInput'

function App() {
  return (
    <>
      <TextInput label={'SomeLabel'} placeholder={'somePlaceHolder'} />
      <TextInput label={'SomeLabel'} placeholder={'somePlaceHolder'} />
    </>
  )
}

export default App
